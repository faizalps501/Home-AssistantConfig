initial_state: 'on'
alias: 'Motion hook'
trigger:
  - platform: webhook
    webhook_id: !secret plex_token
action:
  - service: notify.logfile
    data_template:
      message: >
        Triggered by {{ trigger.webhook_id }}  
        Full data: {{ trigger.json }}
