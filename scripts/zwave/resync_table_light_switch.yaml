alias: "Resync table_light_switch"
sequence:
  - condition: template
    value_template: >-
      {{ states('switch.table_light_switch') != states('binary_sensor.table_light_switch') }}
  - service: notify.logfile
    data_template:
      message: "Resyncing table_light_switch to be {{ states('binary_sensor.table_light_switch') }}"
  - service: rest_command.pi3_zwave
    data_template:
      domain: "switch"
      action: "turn_{{ states('binary_sensor.table_light_switch') }}"
      entity: "switch.table_light_switch"

