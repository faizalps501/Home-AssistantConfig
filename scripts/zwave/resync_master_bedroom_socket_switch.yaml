alias: "Resync master_bedroom_socket_switch"
sequence:
  - condition: template
    value_template: >-
      {{ states('switch.master_bedroom_socket_switch') != states('binary_sensor.master_bedroom_socket_switch') }}
  - service: notify.logfile
    data_template:
      message: "Resyncing master_bedroom_socket_switch to be {{ states('binary_sensor.master_bedroom_socket_switch') }}"
  - service: rest_command.pi3_zwave
    data_template:
      domain: "switch"
      action: "turn_{{ states('binary_sensor.master_bedroom_socket_switch') }}"
      entity: "switch.master_bedroom_socket_switch"

